# Для тестового сервера: именованный том, чтобы при down -v он удалялся и при следующем up init.sql выполнился заново.
services:
  db:
    volumes:
      - db-data-test:/var/lib/postgresql/data
      - ./postgres/init.sql:/docker-entrypoint-initdb.d/init.sql:ro

volumes:
  db-data-test:

